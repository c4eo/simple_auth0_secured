RSpec.describe SimpleAuth0Secured do
  it "has a version number" do
    expect(SimpleAuth0Secured::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
